# Черновики и прошлые версии — остаются на компьютере, но не публикуются,
# чтобы клиент случайно не открыл старую версию по прямой ссылке.
_drafts/

# Исходные скриншоты (40 МБ), из которых нарезаны фото сайта.
# Нужны только при доработке — на сайте используются готовые из assets/img.
assets/source/

# Служебные файлы macOS и редакторов
.DS_Store
._*
.AppleDouble
.LSOverride
Thumbs.db
*.swp
*~

# Настройки Claude Code
.claude/

# Рабочие заметки — не для публичного репозитория
HANDOFF.md

# Папка для ручной загрузки через сайт GitHub — копия того, что публикуется
_upload/

# Материалы от салона: прайсы, тексты, фотографии до обработки.
# Лежат на компьютере, на сайт не публикуются.
_ot-klienta/
